import { requireRole } from "@/lib/authorization"
import { prisma } from "@/lib/prisma"
import { Badge } from "@/components/ui/badge"
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"
export default async function Page(){await requireRole("ADMIN");const items=await prisma.disputeReferral.findMany({orderBy:{createdAt:"desc"}});return <main className="mx-auto flex max-w-6xl flex-col gap-6 p-4 md:p-8"><header><p className="text-sm text-muted-foreground">Intake</p><h1 className="font-serif text-4xl font-semibold">Dispute referrals</h1></header><Card><CardHeader><CardTitle>Referral queue</CardTitle><CardDescription>Review incoming requests before converting them to matters.</CardDescription></CardHeader><CardContent className="flex flex-col gap-3">{items.map(item=><div key={item.id} className="flex flex-col justify-between gap-3 rounded-lg border p-4 sm:flex-row"><div><p className="font-medium">{item.referrerName} · {item.entityType.replaceAll("_"," ")}</p><p className="text-sm text-muted-foreground">{item.referrerEmail}</p><p className="mt-2 text-sm">{item.disputeSummary}</p></div><Badge variant="outline">{item.status.replaceAll("_"," ")}</Badge></div>)}</CardContent></Card></main>}
